import { OppositeDirectionMoveDirective } from './opposite-direction-move.directive';

describe('OppositeDirectionMoveDirective', () => {
  it('should create an instance', () => {
    // const directive = new OppositeDirectionMoveDirective();
    // expect(directive).toBeTruthy();
  });
});
